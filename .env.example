# Public app URL (used for metadata and absolute links)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Supabase (optional — wire up when auth/data are ready)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
